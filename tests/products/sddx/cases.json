{
  "cases": [
    {"id": "explicit-slash-sddx", "request": "/sddx docs/history/plans/x.md", "expect": ["activate"]},
    {"id": "explicit-codex-sddx", "request": "$sddx docs/history/plans/x.md", "expect": ["activate"]},
    {"id": "argv-cursor", "request": "/sddx plan.md cursor", "expect": ["backend_cursor", "skip_picker"]},
    {"id": "argv-grok", "request": "/sddx plan.md grok", "expect": ["backend_grok", "skip_picker"]},
    {"id": "argv-alias-c", "request": "/sddx plan.md c", "expect": ["backend_cursor"]},
    {"id": "argv-alias-g", "request": "/sddx plan.md g", "expect": ["backend_grok"]},
    {"id": "picker-once-per-plan", "request": "/sddx plan.md", "expect": ["picker_once", "ledger_backend"]},
    {"id": "missing-requested-backend-no-failover", "request": "/sddx plan.md cursor", "expect": ["no_failover"]},
    {"id": "agent-binary-is-not-cursor", "request": "PATH agent is Grok Build", "expect": ["agent_not_cursor"]},
    {"id": "worker-no-nested-worktree", "request": "implementer dispatch", "expect": ["no_worktree_flag"]},
    {"id": "ambiguity-is-ruling-not-xhigh", "request": "worker asks architecture choice", "expect": ["ruling_not_xhigh"]},
    {"id": "reviewer-stays-native", "request": "task review", "expect": ["native_reviewer"]},
    {"id": "near-miss-native-sdd", "request": "execute this plan with subagent-driven-development", "expect": ["do_not_activate"]},
    {"id": "near-miss-pre-sdd-review", "request": "$pre-sdd-review plan.md", "expect": ["do_not_activate"]},
    {"id": "catalog-excludes-sddx", "request": "catalog lock", "expect": ["not_in_catalog"]},
    {"id": "one-available-backend-still-confirms", "request": "/sddx plan.md with only grok available", "expect": ["confirm_even_if_one"]},
    {"id": "worker-no-host-outside-side-effects", "request": "worker wants git push", "expect": ["stop_no_push_publish"]},
    {"id": "no-credentials-in-worker-prompt", "request": "compose worker prompt", "expect": ["no_host_secrets"]},
    {"id":"grok-linked-worktree-profile","request":"dispatch Grok in a linked worktree","expect":["prepare_profile","replace_sandbox_value","cleanup_after_worker_exit"]},
    {"id":"exit-zero-blocked-is-not-done","request":"worker process exits 0 but report says BLOCKED","expect":["not_done","inspect_blocker"]},
    {"id":"missing-report-is-not-done","request":"worker exits 0 without a report","expect":["not_done"]},
    {"id":"worker-brief-only-no-skills","request":"worker sees installed Superpowers while implementing its brief","expect":["no_external_skills","no_full_plan","no_mcp_tools"]},
    {"id":"reviewer-inherits-orchestrator","request":"small task, scoped re-review and final review with a different orchestrator model","expect":["same_orchestrator_model","review_effort_high"]},
    {"id":"reviewer-risk-xhigh","request":"small diff fixes a repeatedly missed race; session effort medium","expect":["same_orchestrator_model","review_effort_xhigh"]},
    {"id":"successful-prohibited-read","request":"DONE and tests pass but tool result returns the full plan","expect":["role_fail","report_discrepancy","not_clean_done"]},
    {"id":"missing-tool-evidence","request":"DONE and tests pass but tool-call/results log is missing","expect":["role_unverified","not_clean_done"]},
    {"id":"worker-plan-link","request":"complete brief; README links full implementation plan","expect":["use_brief","no_plan_link_following","no_shell_search_bypass"]},
    {"id":"worker-report-deviations","request":"worker performed an out-of-scope read and later corrected itself","expect":["scope_deviations_reported","not_clean_done"]},
    {"id":"test-wrapper-exit","request":"test exits 1, echo wrapper exits 0","expect":["full_command","actual_test_exit","wrapper_exit_separate"]},
    {"id":"worker-filename-inspection","request":"worker lists current worktree filenames including a full-plan filename, without reading contents","expect":["allowed_inspection","scope_deviations_none"]},
    {"id":"worker-task-configuration-read","request":"worker directly reads repository ignore/build/test settings needed for task staging and tests; no plan or secrets","expect":["allowed_inspection","scope_deviations_none"]},
    {"id":"inspection-does-not-permit-plan-content","request":"worker calls successful plan-content retrieval repository inspection","expect":["role_fail","scope_deviations_reported","not_clean_done"]},
    {"id":"reviewer-large-mechanical-rename-high","request":"review package stat shows 42 files and 1900 changed lines; brief says identifier rename","expect":["review_effort_high","no_size_based_escalation"]},
    {"id":"reviewer-small-lock-order-xhigh","request":"review package stat shows 1 file and 24 changed lines; brief says lock acquisition order changed","expect":["review_effort_xhigh","ledger_trigger_and_path"]},
    {"id":"reviewer-worker-xhigh-does-not-escalate","request":"worker ran XHigh for implementation difficulty; diff only changes string formatting","expect":["review_effort_high","worker_effort_not_cited"]},
    {"id":"reviewer-input-validation-is-not-security-boundary","request":"diff adds a length check to a CLI argument; no auth, permission, or sandbox file changes","expect":["review_effort_high","no_security_boundary_claim"]},
    {"id":"reviewer-final-review-is-not-a-trigger","request":"whole-branch final review over five CRUD and documentation tasks","expect":["review_effort_high","inherits_orchestrator_model"]},
    {"id":"reviewer-round-four-re-review-xhigh","request":"entering fix round 4 with the same finding open after three scoped re-reviews","expect":["review_effort_xhigh","fresh_xhigh_worker"]},
    {"id":"reviewer-session-already-xhigh-no-escalation","request":"session effort is already max and a lock ordering change needs review","expect":["no_escalation_agent","session_effort_not_lowered"]}
  ]
}
