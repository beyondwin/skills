# sample-app message rendering

## Requirements

- Implement `renderMessage(input: string): string` in `src/app.ts`.
- The function returns the rendered string for the supplied input.
- Empty input is rejected.
