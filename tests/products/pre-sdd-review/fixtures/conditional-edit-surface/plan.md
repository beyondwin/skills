# sample-app conditional normalization plan

**Spec:** design.md

## Files

- Read: `src/app.ts`
- Modify: `tests/app.test.ts`

## Implementation

1. If validation finds a missing source label, add it to `src/app.ts`.
2. Run `npm test` and `npm run build`.
